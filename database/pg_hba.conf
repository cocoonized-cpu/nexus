# PostgreSQL Client Authentication Configuration File
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (Unix sockets)
local   all             all                                     trust

# IPv4 local loopback - allow only nexus user
host    all             nexus           127.0.0.1/32            scram-sha-256

# Docker internal network - allow only nexus user from internal services
host    all             nexus           172.0.0.0/8             scram-sha-256
host    all             nexus           10.0.0.0/8              scram-sha-256
host    all             nexus           192.168.0.0/16          scram-sha-256

# Reject all other connections
host    all             all             all                     reject
